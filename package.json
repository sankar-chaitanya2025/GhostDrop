{
  "name": "ghostdrop-root",
  "version": "2.0.0",
  "private": true,
  "description": "Ghost Drop 2.0 Monorepo",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "simulate": "node scripts/simulate-cli.mjs",
    "simulate:web": "open simulator/index.html",
    "lint": "echo 'Checking workspace integrity...'"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "typescript": "^5.8.2",
    "vitest": "^3.0.7"
  }
}
